---
openapi: 3.0.1
info:
  title: Budget API V1
  version: v1
  description: Personal finance budgeting API using the envelope system
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
security:
- bearerAuth: []
paths: {}
servers:
- url: http://localhost:3000
  description: Development server
